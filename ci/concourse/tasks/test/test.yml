---
    platform: linux
    
    image_resource:
      type: docker-image
      source:
        repository: busybox
    inputs:
      - name: articulate-repo
        path: code
    run:        
      path: ./code/ci/concourse/tasks/test/test.sh
    #run:
    #  dir: code
    #  path: ls
    #  args: ["-lRta"]