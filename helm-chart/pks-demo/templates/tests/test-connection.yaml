apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pks-demo.fullname" . }}-test-connection"
  labels:
{{ include "pks-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pks-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
